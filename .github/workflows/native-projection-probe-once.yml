# Copyright (c) 2026 Supratim Sanyal of SANYALnet Labs.
# Proprietary rights reserved except as expressly licensed herein.
#
# ZX-UX Sinclair ZX Spectrum Unix
# This file is governed by the SANYALnet Labs Non-Commercial License in the
# root LICENSE file. Non-Commercial use is permitted; Commercial Use and use
# for AI/ML model training are prohibited unless separately authorized.
#
# Attribution is required: "Based on original work by Supratim Sanyal of
# SANYALnet Labs." See LICENSE for full terms, warranty disclaimer, termination,
# patent, trademark, and governing-law provisions.

name: Native projection listing probe once
# Retry semantic projection after OUT encoder syntax repair.
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/native-projection-probe-once.yml
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      ZXUX_SOURCE_EPOCH: current
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-zxux-runtime
      - name: Build and sample canonical kernel listing
        shell: bash
        run: |
          set -euo pipefail
          test -f v1/dist/certification/R17.00.result.json
          test ! -n "$(git ls-files | grep -Ei '(^|/)(P11(\.|/)|p11[^/]*qualification|phase-?11)' || true)"
          mkdir -p v1/build
          (cd v1/src/kernel && "$GITHUB_WORKSPACE/tools/runtime/sjasmplus/bin/sjasmplus" --nologo --lst=../../build/kernel-probe.lst --sym=../../build/kernel-probe.sym kernel.asm)
          test "$(wc -c < v1/build/kernel.bin)" -eq 8192
          echo "=== LIST HEAD ==="
          sed -n '1,180p' v1/build/kernel-probe.lst
          echo "=== EMITTING SAMPLE ==="
          grep -E '^[[:space:]]*[0-9]+[[:space:]]+[0-9A-Fa-f]{4}[[:space:]]+[0-9A-Fa-f]{2}' v1/build/kernel-probe.lst | head -n 160 || true
          echo "=== LIST TAIL ==="
          tail -n 120 v1/build/kernel-probe.lst
          echo "=== PROJECTION BUILD ==="
          tools/runtime/python/bin/python v1/tools-host/release-tzx/kernel_native_projection.py \
            --listing v1/build/kernel-probe.lst \
            --symbols v1/build/kernel-probe.sym \
            --kernel v1/build/kernel.bin \
            --output v1/build/kernel.nsp \
            --audit v1/build/kernel-nsp.json
          wc -c v1/build/kernel.nsp
          python3 - <<'PY'
          import json
          from pathlib import Path
          d=json.loads(Path('v1/build/kernel-nsp.json').read_text())
          print({k:d[k] for k in ('records','projection_size','text_size','projection_sha256','semantic_reference_sha256','semantic_reference_equals_kernel_oracle')})
          print('first',d['audit'][:12])
          print('last',d['audit'][-12:])
          PY
